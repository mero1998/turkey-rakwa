
import 'package:flutter/material.dart';

class BaseAnimationList extends StatelessWidget {
  const BaseAnimationList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
